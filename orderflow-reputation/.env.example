# PostgreSQL connection (from Railway)
DATABASE_URL=postgresql://user:pass@host:5432/railway

# Calculation interval in seconds (default: 3600 = 1 hour)
CALCULATION_INTERVAL_SECONDS=3600

# Logging
RUST_LOG=info,orderflow_reputation=debug
